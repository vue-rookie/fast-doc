# 欢迎使用 API 文档中心

这是一个专为开发者设计的文档门户。支持标准的 Markdown 渲染、代码高亮以及 API 调试说明。

### 核心特性

- **多语言支持**：原生支持 Python, Node.js, Go 等多种语言示例。
- **兼容性**：完全兼容 OpenAI 接口规范。
- **高性能**：全球节点加速，平均延迟 < 200ms。

### 快速接入

在您的终端中运行以下命令以测试连接：

```bash
curl https://api.example.com/v1/models \
  -H "Authorization: Bearer YOUR_API_KEY"
```